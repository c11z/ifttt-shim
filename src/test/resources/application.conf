akka {
  loglevel = INFO
  event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
}


strava {
  athlete-activity-limit = "20"

  secret {
    ifttt-channel-key = "ThisIsAValidChannelKey"
  }
  test {
    setup-response = """{"data": {"accessToken": "NotARealToken", "samples": {}}}"""
    access-token = "NotARealToken"
  }
}
